
// // const selectElem = document.querySelector("#select-item");
// // const formElem = document.querySelector("#country-compare");

// // selectElem.addEventListener("click", () => {
// //    let options = selectElem.querySelectorAll("option");
// //    let count = options.length;
// //    if(count > 2) {
// //        formElem.setAttribute("action", "/compare-countries-multi");
// //    } else {
// //     formElem.setAttribute("action", "/compare-countries");
// //    }

// // });

// const selectElem = document.querySelector("#select-item");
// const formElem = document.querySelector("#country-compare");

// selectElem.addEventListener("change", () => {
//     // Get the number of selected options
//     let selectedOptions = selectElem.selectedOptions;
//     let count = selectedOptions.length;

//     // Update the form's action attribute based on the count
//     if (count > 2) {
//         formElem.setAttribute("action", "/compare-countries-multi");
//     } else {
//         formElem.setAttribute("action", "/compare-countries");
//     }
// });
